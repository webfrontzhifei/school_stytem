LBF.config({
	alias: {
		globalSettings: loaderDir + "globalSettings"
	},
	vars: {
		theme: loaderDir + "ui/theme/default"
	},
	paths:{
		app: loaderDir + 'app',
		lang: loaderDir + 'lang',
		monitor: loaderDir + 'monitor',
		lib: loaderDir + 'lib',
		ui: loaderDir + 'ui',
		util: loaderDir + 'util'
	}
});
